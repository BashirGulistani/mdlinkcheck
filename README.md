# mdlinkcheck

Fast Markdown link checker for repos. Detects broken HTTP(S) links and broken local file links referenced from Markdown.

## Why this exists

Broken links in READMEs and docs waste time and reduce trust. This tool is small, dependency-light, and CI-friendly.

## Install

```bash
python -m pip install -e .
